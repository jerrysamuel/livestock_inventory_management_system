asgiref==3.7.2
Django==4.2.5
django-crispy-forms==1.14.0
Pillow==10.0.0
sqlparse==0.4.4
tzdata==2023.3
python==3.11.4

#There is a myenv file attached to the app folder, amd it should be activated before 
#running the server
#This file comtains the necessary apps required to run the app. you need to have python installed on your machine before you can be able to run it.
#To install them run (pip install -r requirements.txt)
